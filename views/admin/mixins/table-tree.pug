mixin table-tree(items, level = 1)

  each item, index in items

    - const prefix = Array(level + 1).join("- ")

    tr

      td
        input(type="checkbox" name="id" value=item.id)

      td #{item.index}

      td
        img.rounded.shadow-sm(
          src=item.thumbnail
          alt=item.title
          width="80"
          height="80"
          style="object-fit:cover"
        )

      td
        span.category-title #{prefix}#{item.title}

      td
        input.form-control.form-control-sm.position-input(
          type="number"
          value=item.position
          min="1"
        )

      td
        if(item.status == "active")
          span.badge.status-active Hoạt động
        else
          span.badge.status-stop Dừng

      td
        .action-buttons

          a.btn.btn-sm.btn-view.me-1(
            href=`${prefixAdmin}/products-category/detail/${item.id}`
          )
            i.fas.fa-eye

          if(role.permissions.includes("products-category_edit"))
            a.btn.btn-sm.btn-edit.me-1(
              href=`${prefixAdmin}/products-category/edit/${item.id}`
            )
              i.fas.fa-pen

          if(role.permissions.includes("products-category_delete"))
            button.btn.btn-sm.btn-delete.me-1(
              button-delete
              data-id=item.id
            )
              i.fa-solid.fa-trash

    if item.children && item.children.length > 0
      +table-tree(item.children, level + 1)