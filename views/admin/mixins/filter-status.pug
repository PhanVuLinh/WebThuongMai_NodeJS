mixin filter-status(filterStatus)
  .filter-status
    each item in filterStatus
      button.btn.btn-sm.me-2.filter-btn(
        class=item.class
        button-status=item.status
      )
        i.fas.fa-circle.me-1
        | #{item.name}